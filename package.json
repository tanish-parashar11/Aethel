{
  "name": "aneis-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @aneis/web\" \"npm run dev --workspace @aneis/api\"",
    "build": "npm run build --workspace @aneis/web && npm run build --workspace @aneis/api",
    "lint": "npm run lint --workspace @aneis/web && npm run lint --workspace @aneis/api",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
